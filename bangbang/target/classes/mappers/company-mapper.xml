<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper PUBLIC
"-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="companyMapper">

	<select id="loginCheck" parameterType="string" resultType="company" >
		<![CDATA[
		select * from company
		where com_id = #{member_id} 
		]]>
	</select>
	
<!-- 	<select id="selectMember" parameterType="string" >
		select * from company 
		where com_id = #{  }
	</select>
	
	<select id="selectList" >
		select * from company
	</select>
	
	<insert id="insertCompany" parameterType="company" useGeneratedKeys="true">
		insert into company 
		values(#{  }, #{  }, #{  }, 
			#{  }, #{  }, #{  }, #{  }, #{  }, #{  })
	</insert>
	todo : insert 시 member 도 필요
	
	<delete id="deleteCompany" parameterType="String">
		delete from company where com_id = #{  }
	</delete>
	
	<update id="updateCompany" parameterType="company" >
		update company 
		set com_name = #{  }, com_ceo = #{  }, 
		com_no = #{  }, brokers_no = #{  }, 
		brokers_address = #{  }, com_phone = #{  }, 
		com_content = #{  }, com_email = #{  } 
		where com_id = #{  }
	</update> -->
	

</mapper>



